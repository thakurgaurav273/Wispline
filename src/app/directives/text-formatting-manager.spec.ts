import { TextFormattingManager } from './text-formatting-manager';

describe('TextFormattingManager', () => {
  it('should create an instance', () => {
    const directive = new TextFormattingManager();
    expect(directive).toBeTruthy();
  });
});
