import { MentionsFormatter } from './mentions-formatter';

describe('MentionsFormatter', () => {
  it('should create an instance', () => {
    const directive = new MentionsFormatter();
    expect(directive).toBeTruthy();
  });
});
