import { BaseTextFormatter } from './base-text-formatter';

describe('BaseTextFormatter', () => {
  it('should create an instance', () => {
    const directive = new BaseTextFormatter();
    expect(directive).toBeTruthy();
  });
});
