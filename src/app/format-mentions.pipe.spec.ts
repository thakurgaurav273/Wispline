import { FormatMentionsPipe } from './format-mentions.pipe';

describe('FormatMentionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMentionsPipe();
    expect(pipe).toBeTruthy();
  });
});
