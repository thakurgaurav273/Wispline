import { TextFormatterDirective } from './text-formatter.directive';

describe('TextFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
