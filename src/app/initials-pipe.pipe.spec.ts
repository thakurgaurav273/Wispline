import { InitialsPipePipe } from './initials-pipe.pipe';

describe('InitialsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
